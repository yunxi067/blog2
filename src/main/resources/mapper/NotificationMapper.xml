<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itzhihao.blog.mapper.NotificationMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, from_user_id, title, content, type, status, create_time)
        VALUES (#{userId}, #{fromUserId}, #{title}, #{content}, #{type}, #{status}, #{createTime})
    </insert>

    <select id="findById" resultType="com.itzhihao.blog.entity.Notification">
        SELECT * FROM notification WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="com.itzhihao.blog.entity.Notification">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findUnreadByUserId" resultType="com.itzhihao.blog.entity.Notification">
        SELECT * FROM notification WHERE user_id = #{userId} AND status = 0 ORDER BY create_time DESC
    </select>

    <update id="markAsRead">
        UPDATE notification SET status = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE notification SET status = 1 WHERE user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM notification WHERE id = #{id}
    </delete>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND status = 0
    </select>

</mapper>