<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itzhihao.blog.mapper.FollowMapper">

    <insert id="insert">
        INSERT INTO follow (follower_id, following_id, create_time)
        VALUES (#{followerId}, #{followingId}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM follow WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </delete>

    <select id="isFollowing" resultType="boolean">
        SELECT COUNT(*) > 0 FROM follow 
        WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </select>

    <select id="findFollowers" resultType="com.itzhihao.blog.entity.Follow">
        SELECT * FROM follow WHERE following_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findFollowing" resultType="com.itzhihao.blog.entity.Follow">
        SELECT * FROM follow WHERE follower_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM follow WHERE following_id = #{userId}
    </select>

    <select id="countFollowing" resultType="int">
        SELECT COUNT(*) FROM follow WHERE follower_id = #{userId}
    </select>

</mapper>